{
  "name": "vll-video-language-learner",
  "private": true,
  "version": "1.3.4",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "eslint .",
    "check": "npm run lint && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint": "^9.39.4"
  }
}
