{
  "name": "vll-video-language-learner",
  "private": true,
  "version": "1.4.3",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "eslint .",
    "check": "npm run lint && npm run test",
    "package": "node scripts/package.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint": "^9.39.4"
  }
}